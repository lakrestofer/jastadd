import java.util.LinkedList;

aspect Test {
  inh Root A.root();

  eq Root.getA().root() = this;

  coll LinkedList<String> Root.names() [new LinkedList<>()] with add;

  A contributes "A" when true to Root.names() for root();

  B contributes "B" when hasA() to Root.names() for root();
}
