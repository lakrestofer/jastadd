import java.util.LinkedList;

aspect Test {
  coll LinkedList<B> R.bList();

  // Custom survey code is inserted into generated collection survey method.
  A contributes {
    implicitB().collectContributions();
    super.collectContributions();
  } to R.bList();

  B contributes this to R.bList();

  syn nta B A.implicitB() = new B();
}
