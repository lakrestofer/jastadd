// Inherited lookup.
aspect Test123 {
  inh B B.lookup(String name);

  eq A.getB(int index).lookup(String name) {
    for (int i = 0; i < getNumB(); i++) {
      B b = getB(i);
      if (b.getName().equals(name)) {
        return b;
      }
    }
    return null;
  }

  syn lazy B B.decl() = lookup(getUse());

  // Additional attribute unrelated to the above name lookup.
  syn lazy Value<String> A.a() = Value.of(getC().getName());
}
