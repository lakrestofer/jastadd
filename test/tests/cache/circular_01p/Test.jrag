aspect Original {
  syn lazy int A.x() = y();

  syn int A.y() = 3;
}

aspect Extension {
  syn int B.y() circular [3] = Math.min(10, x() + 1);

  uncache A.x(); // A.x() must not be cached, since B.y() depends circularly on it.
}
